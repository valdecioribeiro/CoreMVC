@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model IEnumerable<CoreMVC.Models.Departamento>
@{
    ViewData["Title"] = "Departamentos";
}

<h2>Departamentos</h2>

<p>
    <a asp-action="Create">Criar novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (Departamento item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Editar", "Editar", new { /* id=departamento.PrimaryKey*/})
                @Html.ActionLink("Detalhar", "Detalhar", new { /* id=departamento.PrimaryKey*/})
                @Html.ActionLink("Deletar", "Deletar", new { /* id=departamento.PrimaryKey*/})
            </td>
        </tr>
        }
    </tbody>
</table>
